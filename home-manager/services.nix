{
  services = {
    # avizo.enable = true;

    mako = {
      enable = true;

      settings = {
        anchor = "top-right";
        defaultTimeout = "30000"; # milliseconds
      };
    };
  };
}
